// Altere apenas esse IP quando mudar de rede ou computador
export const SERVER_IP = "192.168.1.104"; // <- seu IP local atual
export const SERVER_PORT = 8080;

// Caminho base para todas as chamadas da API
export const API_BASE_URL = `http://${SERVER_IP}:${SERVER_PORT}/api`;
